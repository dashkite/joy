name: rest
type: function
summary: >-
  Takes all but the first an element of an array.
signatures:
  - arguments:
      - name: array
        type: Array
        description: The array from which to take elements.
    returns:
      name: array
      type: Array
      description: The resulting array.
