title: instanceOf
name: instanceOf
type: function
summary: >-
  Returns true if `instanceof` returns true for the given instance and type,
  false otherwise. Convenience wrapper for `instanceof`. However,
  [`isKind`](#iskind) is less likely to throw.
signatures:
  - arguments:
      - name: type
      - name: instance
    returns:
      name: boolean
