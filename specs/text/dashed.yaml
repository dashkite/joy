name: dashed
aliases:
  - dash
  - dashes
type: function
summary: Converts a space-delimited string to hyphen-delimited.
signatures:
  - arguments:
      - name: string
        type: String
        description: The string to convert.
    returns:
      name: string
      type: String
      description: The modified string.
