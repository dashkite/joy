title: all
name: all
type: function
summary: >-
  Returns a promise that resolve to an array of the values of of the resolved
  promises. Rejects if any of the promises are rejected.
signatures:
  - arguments:
      - name: iterable
    returns:
      name: promise
